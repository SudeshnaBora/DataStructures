It contains the linked list data structure.

The driver program is the mainProgram.c

All the functions are present in LinkedList.h

However, for individual functions - refer to the functionname.c file . It contains the definition of the function.
